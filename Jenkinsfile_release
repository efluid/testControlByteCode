node('socle-jenkins-maven-docker-14-4G') {

  checkout scm

  try{
    env.JAVA_HOME = "${variables.jdkPathPrefix}11.0.1"
    sh "mvn -B release:prepare release:perform -DreleaseVersion=${releaseVersion} -DdevelopmentVersion=${developmentVersion}"
    sh 'git status'
    sh 'git diff'
    //sh 'git push origin master'
  } catch (Exception e) {
    //TODO send mail
    throw e
  }
}
return this;
